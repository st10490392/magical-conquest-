# data/

Runtime data for the prototype. Save games are written to `data/saves/`
(ignored by Git). Future milestones may add small JSON configuration files
here (enemy definitions, progression curves).
